apiVersion: duck/v1
kind: ComputeAssignmentList
assignments:
  - endpoint: local-dev
    principal: __DUCK_DEV_BOOTSTRAP_PRINCIPAL__
    principal_type: user
    is_default: true
  - endpoint: local-dev
    principal: analyst
    principal_type: user
    is_default: true
