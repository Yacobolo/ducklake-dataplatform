apiVersion: duck/v1
kind: Table
metadata:
  name: raw_movies
spec:
  table_type: MANAGED
  comment: Raw movie dimension loaded through ingestion API
  owner: ml_admin
  columns:
    - name: movie_id
      type: int64
    - name: title
      type: varchar
    - name: genres
      type: varchar
