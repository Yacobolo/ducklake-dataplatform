apiVersion: duck/v1
kind: Table
metadata:
  name: raw_users
spec:
  table_type: MANAGED
  comment: Raw user dimension with policy-controlled access
  owner: ml_admin
  columns:
    - name: user_id
      type: int64
    - name: age_group
      type: varchar
    - name: region
      type: varchar
