// Code generated by cli-gen. DO NOT EDIT.
package gen

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newObservabilityCmd(client *Client) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "observability",
		Short: "Audit logs, query history, and metastore info",
	}

	auditLogsCmd := &cobra.Command{
		Use:   "audit-logs",
		Short: "Manage audit-logs",
	}
	cmd.AddCommand(auditLogsCmd)

	metastoreCmd := &cobra.Command{
		Use:   "metastore",
		Short: "Manage metastore",
	}
	cmd.AddCommand(metastoreCmd)

	queryHistoryCmd := &cobra.Command{
		Use:   "query-history",
		Short: "Manage query-history",
	}
	cmd.AddCommand(queryHistoryCmd)

	// listAuditLogs
	{
		c := &cobra.Command{
			Use:   "list",
			Short: "Query audit logs",
			Long:  "Retrieves paginated audit log entries, optionally filtered by principal, action, or status.",
			RunE: func(cmd *cobra.Command, args []string) error {
				outputFlag, _ := cmd.Flags().GetString("output")
				_ = outputFlag
				urlPath := "/audit-logs"
				query := url.Values{}
				if cmd.Flags().Changed("action") {
					v, _ := cmd.Flags().GetString("action")
					query.Set("action", v)
				}
				if cmd.Flags().Changed("max-results") {
					v, _ := cmd.Flags().GetInt64("max-results")
					query.Set("max_results", fmt.Sprintf("%d", v))
				}
				if cmd.Flags().Changed("page-token") {
					v, _ := cmd.Flags().GetString("page-token")
					query.Set("page_token", v)
				}
				if cmd.Flags().Changed("principal-name") {
					v, _ := cmd.Flags().GetString("principal-name")
					query.Set("principal_name", v)
				}
				if cmd.Flags().Changed("status") {
					v, _ := cmd.Flags().GetString("status")
					query.Set("status", v)
				}

				// Execute request
				resp, err := client.Do("GET", urlPath, query, nil)
				if err != nil {
					return err
				}
				if err := CheckError(resp); err != nil {
					return err
				}
				respBody, err := ReadBody(resp)
				if err != nil {
					return fmt.Errorf("read response: %w", err)
				}

				// Handle --quiet
				quiet, _ := cmd.Root().PersistentFlags().GetBool("quiet")
				if quiet {
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err == nil {
						// Try common ID fields
						for _, key := range []string{"id", "name", "key"} {
							if v, ok := data[key]; ok {
								fmt.Fprintln(os.Stdout, v)
								return nil
							}
						}
					}
					fmt.Fprintln(os.Stdout, string(respBody))
					return nil
				}

				switch OutputFormat(outputFlag) {
				case OutputJSON:
					var pretty interface{}
					json.Unmarshal(respBody, &pretty)
					return PrintJSON(os.Stdout, pretty)
				default:
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err != nil {
						return fmt.Errorf("parse response: %w", err)
					}
					columns := []string{"id", "principal_name", "action", "status", "created_at"}
					rows := ExtractRows(data, columns)
					PrintTable(os.Stdout, columns, rows)
				}
				return nil
			},
		}
		c.Flags().String("action", "", "Filter by audit action type.")
		c.Flags().Int64("max-results", 100, "Maximum number of results to return per page.")
		c.Flags().String("page-token", "", "Opaque pagination token from a previous response.")
		c.Flags().String("principal-name", "", "Filter by principal name.")
		c.Flags().String("status", "", "Filter by status.")

		// Apply overrides
		if fn, ok := runOverrides["listAuditLogs"]; ok {
			c.RunE = fn(client)
		}
		if fn, ok := commandOverrides["listAuditLogs"]; ok {
			fn(c)
		}
		auditLogsCmd.AddCommand(c)
	}

	// listQueryHistory
	{
		c := &cobra.Command{
			Use:   "list",
			Short: "List query execution history",
			Long:  "Retrieves paginated query execution history, optionally filtered by principal, status, or time range.",
			RunE: func(cmd *cobra.Command, args []string) error {
				outputFlag, _ := cmd.Flags().GetString("output")
				_ = outputFlag
				urlPath := "/query-history"
				query := url.Values{}
				if cmd.Flags().Changed("from") {
					v, _ := cmd.Flags().GetString("from")
					query.Set("from", v)
				}
				if cmd.Flags().Changed("max-results") {
					v, _ := cmd.Flags().GetInt64("max-results")
					query.Set("max_results", fmt.Sprintf("%d", v))
				}
				if cmd.Flags().Changed("page-token") {
					v, _ := cmd.Flags().GetString("page-token")
					query.Set("page_token", v)
				}
				if cmd.Flags().Changed("principal-name") {
					v, _ := cmd.Flags().GetString("principal-name")
					query.Set("principal_name", v)
				}
				if cmd.Flags().Changed("status") {
					v, _ := cmd.Flags().GetString("status")
					query.Set("status", v)
				}
				if cmd.Flags().Changed("to") {
					v, _ := cmd.Flags().GetString("to")
					query.Set("to", v)
				}

				// Execute request
				resp, err := client.Do("GET", urlPath, query, nil)
				if err != nil {
					return err
				}
				if err := CheckError(resp); err != nil {
					return err
				}
				respBody, err := ReadBody(resp)
				if err != nil {
					return fmt.Errorf("read response: %w", err)
				}

				// Handle --quiet
				quiet, _ := cmd.Root().PersistentFlags().GetBool("quiet")
				if quiet {
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err == nil {
						// Try common ID fields
						for _, key := range []string{"id", "name", "key"} {
							if v, ok := data[key]; ok {
								fmt.Fprintln(os.Stdout, v)
								return nil
							}
						}
					}
					fmt.Fprintln(os.Stdout, string(respBody))
					return nil
				}

				switch OutputFormat(outputFlag) {
				case OutputJSON:
					var pretty interface{}
					json.Unmarshal(respBody, &pretty)
					return PrintJSON(os.Stdout, pretty)
				default:
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err != nil {
						return fmt.Errorf("parse response: %w", err)
					}
					columns := []string{"id", "principal_name", "status", "duration_ms", "created_at"}
					rows := ExtractRows(data, columns)
					PrintTable(os.Stdout, columns, rows)
				}
				return nil
			},
		}
		c.Flags().String("from", "", "Start of the time range filter (ISO 8601 datetime).")
		c.Flags().Int64("max-results", 100, "Maximum number of results to return per page.")
		c.Flags().String("page-token", "", "Opaque pagination token from a previous response.")
		c.Flags().String("principal-name", "", "Filter by principal name.")
		c.Flags().String("status", "", "Filter by status.")
		c.Flags().String("to", "", "End of the time range filter (ISO 8601 datetime).")

		// Apply overrides
		if fn, ok := runOverrides["listQueryHistory"]; ok {
			c.RunE = fn(client)
		}
		if fn, ok := commandOverrides["listQueryHistory"]; ok {
			fn(c)
		}
		queryHistoryCmd.AddCommand(c)
	}

	// getMetastoreSummary
	{
		c := &cobra.Command{
			Use:   "summary",
			Short: "Get metastore summary",
			Long:  "Returns high-level information about the DuckLake metastore.",
			RunE: func(cmd *cobra.Command, args []string) error {
				outputFlag, _ := cmd.Flags().GetString("output")
				_ = outputFlag
				urlPath := "/catalogs/{catalogName}/metastore/summary"
				query := url.Values{}
				if cmd.Flags().Changed("catalog-name") {
					v, _ := cmd.Flags().GetString("catalog-name")
					query.Set("catalogName", v)
				}

				// Execute request
				resp, err := client.Do("GET", urlPath, query, nil)
				if err != nil {
					return err
				}
				if err := CheckError(resp); err != nil {
					return err
				}
				respBody, err := ReadBody(resp)
				if err != nil {
					return fmt.Errorf("read response: %w", err)
				}

				// Handle --quiet
				quiet, _ := cmd.Root().PersistentFlags().GetBool("quiet")
				if quiet {
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err == nil {
						// Try common ID fields
						for _, key := range []string{"id", "name", "key"} {
							if v, ok := data[key]; ok {
								fmt.Fprintln(os.Stdout, v)
								return nil
							}
						}
					}
					fmt.Fprintln(os.Stdout, string(respBody))
					return nil
				}

				switch OutputFormat(outputFlag) {
				case OutputJSON:
					var pretty interface{}
					json.Unmarshal(respBody, &pretty)
					return PrintJSON(os.Stdout, pretty)
				default:
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err != nil {
						return fmt.Errorf("parse response: %w", err)
					}
					PrintDetail(os.Stdout, data)
				}
				return nil
			},
		}
		c.Flags().String("catalog-name", "", "Name of the catalog.")
		_ = c.MarkFlagRequired("catalog-name")

		// Apply overrides
		if fn, ok := runOverrides["getMetastoreSummary"]; ok {
			c.RunE = fn(client)
		}
		if fn, ok := commandOverrides["getMetastoreSummary"]; ok {
			fn(c)
		}
		metastoreCmd.AddCommand(c)
	}

	return cmd
}
