// Code generated by cli-gen. DO NOT EDIT.
package gen

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"
	"strings"

	"github.com/spf13/cobra"
)

func newIngestionCmd(client *Client) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "ingestion",
		Short: "Data ingestion operations",
	}

	// commitTableIngestion
	{
		c := &cobra.Command{
			Use:     "commit <schema-name> <table-name>",
			Short:   "Register uploaded Parquet files in DuckLake",
			Long:    "Registers previously uploaded files (from upload-url) in the DuckLake catalog. DuckLake validates schema compatibility, extracts column statistics, and registers files atomically. Requires INSERT privilege on the target table.\n",
			Example: "duck ingestion commit <schema-name> <table-name> --ignore-extra-columns --s3-keys uploads/2025/01/events-part1.parquet,uploads/2025/01/events-part2.parquet",
			Args:    cobra.ExactArgs(2),
			RunE: func(cmd *cobra.Command, args []string) error {
				outputFlag, _ := cmd.Flags().GetString("output")
				_ = outputFlag
				urlPath := "/catalogs/{catalogName}/schemas/{schemaName}/tables/{tableName}/ingestion/commit"
				urlPath = strings.Replace(urlPath, "{schemaName}", args[0], 1)
				urlPath = strings.Replace(urlPath, "{tableName}", args[1], 1)
				query := url.Values{}
				if cmd.Flags().Changed("catalog-name") {
					v, _ := cmd.Flags().GetString("catalog-name")
					query.Set("catalogName", v)
				}
				// Build request body
				var body interface{}
				jsonInput, _ := cmd.Flags().GetString("json")
				if jsonInput != "" {
					var raw interface{}
					jsonData := jsonInput
					if jsonInput == "-" {
						data, err := os.ReadFile("/dev/stdin")
						if err != nil {
							return fmt.Errorf("read stdin: %w", err)
						}
						jsonData = string(data)
					} else if strings.HasPrefix(jsonInput, "@") {
						data, err := os.ReadFile(jsonInput[1:])
						if err != nil {
							return fmt.Errorf("read file: %w", err)
						}
						jsonData = string(data)
					}
					if err := json.Unmarshal([]byte(jsonData), &raw); err != nil {
						return fmt.Errorf("parse JSON input: %w", err)
					}
					body = raw
				} else {
					m := map[string]interface{}{}
					if cmd.Flags().Changed("allow-missing-columns") {
						v, _ := cmd.Flags().GetBool("allow-missing-columns")
						parts := strings.SplitN("options.allow_missing_columns", ".", 2)
						if m[parts[0]] == nil {
							m[parts[0]] = map[string]interface{}{}
						}
						m[parts[0]].(map[string]interface{})[parts[1]] = v
					}
					if cmd.Flags().Changed("ignore-extra-columns") {
						v, _ := cmd.Flags().GetBool("ignore-extra-columns")
						parts := strings.SplitN("options.ignore_extra_columns", ".", 2)
						if m[parts[0]] == nil {
							m[parts[0]] = map[string]interface{}{}
						}
						m[parts[0]].(map[string]interface{})[parts[1]] = v
					}
					if cmd.Flags().Changed("s3-keys") {
						v, _ := cmd.Flags().GetStringSlice("s3-keys")
						m["s3_keys"] = v
					}
					if len(m) > 0 {
						body = m
					}
				}

				// Execute request
				resp, err := client.Do("POST", urlPath, query, body)
				if err != nil {
					return err
				}
				if err := CheckError(resp); err != nil {
					return err
				}
				respBody, err := ReadBody(resp)
				if err != nil {
					return fmt.Errorf("read response: %w", err)
				}

				// Handle --quiet
				quiet, _ := cmd.Root().PersistentFlags().GetBool("quiet")
				if quiet {
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err == nil {
						// Handle paginated list responses ({"data": [...]})
						if items, ok := data["data"].([]interface{}); ok {
							for _, item := range items {
								if m, ok := item.(map[string]interface{}); ok {
									for _, key := range []string{"id", "name", "key"} {
										if v, ok := m[key]; ok {
											fmt.Fprintln(os.Stdout, v)
											break
										}
									}
								}
							}
							return nil
						}
						// Handle single resource responses
						for _, key := range []string{"id", "name", "key"} {
							if v, ok := data[key]; ok {
								fmt.Fprintln(os.Stdout, v)
								return nil
							}
						}
					}
					fmt.Fprintln(os.Stdout, string(respBody))
					return nil
				}

				switch OutputFormat(outputFlag) {
				case OutputJSON:
					var pretty interface{}
					json.Unmarshal(respBody, &pretty)
					return PrintJSON(os.Stdout, pretty)
				default:
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err != nil {
						return fmt.Errorf("parse response: %w", err)
					}
					PrintDetail(os.Stdout, data)
				}
				return nil
			},
		}
		c.Flags().Bool("allow-missing-columns", false, "Allow columns in the table that are missing from the Parquet file.")
		c.Flags().String("catalog-name", "", "Name of the catalog.")
		_ = c.MarkFlagRequired("catalog-name")
		c.Flags().Bool("ignore-extra-columns", false, "Ignore columns in the Parquet file not present in the table.")
		c.Flags().String("json", "", "JSON input (raw string or @filename or - for stdin)")
		c.Flags().StringSlice("s3-keys", nil, "S3 keys returned from upload-url to register.")
		_ = c.MarkFlagRequired("s3-keys")

		// Apply overrides
		if fn, ok := runOverrides["commitTableIngestion"]; ok {
			c.RunE = fn(client)
		}
		if fn, ok := commandOverrides["commitTableIngestion"]; ok {
			fn(c)
		}
		cmd.AddCommand(c)
	}

	// loadTableExternalFiles
	{
		c := &cobra.Command{
			Use:     "load <schema-name> <table-name>",
			Short:   "Register existing S3 files in DuckLake",
			Long:    "Registers existing S3 files or globs in the DuckLake catalog. Paths without an s3:// prefix are treated as relative to the lake data path. DuckLake validates schema compatibility, extracts column statistics, and registers files atomically. Requires INSERT privilege on the target table.\n",
			Example: "duck ingestion load <schema-name> <table-name> --paths s3://acme-datalake/raw/events/2025/01/*.parquet",
			Args:    cobra.ExactArgs(2),
			RunE: func(cmd *cobra.Command, args []string) error {
				outputFlag, _ := cmd.Flags().GetString("output")
				_ = outputFlag
				urlPath := "/catalogs/{catalogName}/schemas/{schemaName}/tables/{tableName}/ingestion/load"
				urlPath = strings.Replace(urlPath, "{schemaName}", args[0], 1)
				urlPath = strings.Replace(urlPath, "{tableName}", args[1], 1)
				query := url.Values{}
				if cmd.Flags().Changed("catalog-name") {
					v, _ := cmd.Flags().GetString("catalog-name")
					query.Set("catalogName", v)
				}
				// Build request body
				var body interface{}
				jsonInput, _ := cmd.Flags().GetString("json")
				if jsonInput != "" {
					var raw interface{}
					jsonData := jsonInput
					if jsonInput == "-" {
						data, err := os.ReadFile("/dev/stdin")
						if err != nil {
							return fmt.Errorf("read stdin: %w", err)
						}
						jsonData = string(data)
					} else if strings.HasPrefix(jsonInput, "@") {
						data, err := os.ReadFile(jsonInput[1:])
						if err != nil {
							return fmt.Errorf("read file: %w", err)
						}
						jsonData = string(data)
					}
					if err := json.Unmarshal([]byte(jsonData), &raw); err != nil {
						return fmt.Errorf("parse JSON input: %w", err)
					}
					body = raw
				} else {
					m := map[string]interface{}{}
					if cmd.Flags().Changed("allow-missing-columns") {
						v, _ := cmd.Flags().GetBool("allow-missing-columns")
						parts := strings.SplitN("options.allow_missing_columns", ".", 2)
						if m[parts[0]] == nil {
							m[parts[0]] = map[string]interface{}{}
						}
						m[parts[0]].(map[string]interface{})[parts[1]] = v
					}
					if cmd.Flags().Changed("ignore-extra-columns") {
						v, _ := cmd.Flags().GetBool("ignore-extra-columns")
						parts := strings.SplitN("options.ignore_extra_columns", ".", 2)
						if m[parts[0]] == nil {
							m[parts[0]] = map[string]interface{}{}
						}
						m[parts[0]].(map[string]interface{})[parts[1]] = v
					}
					if cmd.Flags().Changed("paths") {
						v, _ := cmd.Flags().GetStringSlice("paths")
						m["paths"] = v
					}
					if len(m) > 0 {
						body = m
					}
				}

				// Execute request
				resp, err := client.Do("POST", urlPath, query, body)
				if err != nil {
					return err
				}
				if err := CheckError(resp); err != nil {
					return err
				}
				respBody, err := ReadBody(resp)
				if err != nil {
					return fmt.Errorf("read response: %w", err)
				}

				// Handle --quiet
				quiet, _ := cmd.Root().PersistentFlags().GetBool("quiet")
				if quiet {
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err == nil {
						// Handle paginated list responses ({"data": [...]})
						if items, ok := data["data"].([]interface{}); ok {
							for _, item := range items {
								if m, ok := item.(map[string]interface{}); ok {
									for _, key := range []string{"id", "name", "key"} {
										if v, ok := m[key]; ok {
											fmt.Fprintln(os.Stdout, v)
											break
										}
									}
								}
							}
							return nil
						}
						// Handle single resource responses
						for _, key := range []string{"id", "name", "key"} {
							if v, ok := data[key]; ok {
								fmt.Fprintln(os.Stdout, v)
								return nil
							}
						}
					}
					fmt.Fprintln(os.Stdout, string(respBody))
					return nil
				}

				switch OutputFormat(outputFlag) {
				case OutputJSON:
					var pretty interface{}
					json.Unmarshal(respBody, &pretty)
					return PrintJSON(os.Stdout, pretty)
				default:
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err != nil {
						return fmt.Errorf("parse response: %w", err)
					}
					PrintDetail(os.Stdout, data)
				}
				return nil
			},
		}
		c.Flags().Bool("allow-missing-columns", false, "Allow columns in the table that are missing from the Parquet file.")
		c.Flags().String("catalog-name", "", "Name of the catalog.")
		_ = c.MarkFlagRequired("catalog-name")
		c.Flags().Bool("ignore-extra-columns", false, "Ignore columns in the Parquet file not present in the table.")
		c.Flags().String("json", "", "JSON input (raw string or @filename or - for stdin)")
		c.Flags().StringSlice("paths", nil, "S3 paths or globs to register.")
		_ = c.MarkFlagRequired("paths")

		// Apply overrides
		if fn, ok := runOverrides["loadTableExternalFiles"]; ok {
			c.RunE = fn(client)
		}
		if fn, ok := commandOverrides["loadTableExternalFiles"]; ok {
			fn(c)
		}
		cmd.AddCommand(c)
	}

	// createUploadUrl
	{
		c := &cobra.Command{
			Use:     "upload-url <schema-name> <table-name>",
			Short:   "Get a presigned URL for uploading a Parquet file",
			Long:    "Returns a presigned PUT URL that the client can use to upload a Parquet file directly to S3. The file can then be registered via the commit endpoint. Requires INSERT privilege on the target table.\n",
			Example: "duck ingestion upload-url <schema-name> <table-name> --filename events-2025-01-15.parquet",
			Args:    cobra.ExactArgs(2),
			RunE: func(cmd *cobra.Command, args []string) error {
				outputFlag, _ := cmd.Flags().GetString("output")
				_ = outputFlag
				urlPath := "/catalogs/{catalogName}/schemas/{schemaName}/tables/{tableName}/ingestion/upload-url"
				urlPath = strings.Replace(urlPath, "{schemaName}", args[0], 1)
				urlPath = strings.Replace(urlPath, "{tableName}", args[1], 1)
				query := url.Values{}
				if cmd.Flags().Changed("catalog-name") {
					v, _ := cmd.Flags().GetString("catalog-name")
					query.Set("catalogName", v)
				}
				// Build request body
				var body interface{}
				jsonInput, _ := cmd.Flags().GetString("json")
				if jsonInput != "" {
					var raw interface{}
					jsonData := jsonInput
					if jsonInput == "-" {
						data, err := os.ReadFile("/dev/stdin")
						if err != nil {
							return fmt.Errorf("read stdin: %w", err)
						}
						jsonData = string(data)
					} else if strings.HasPrefix(jsonInput, "@") {
						data, err := os.ReadFile(jsonInput[1:])
						if err != nil {
							return fmt.Errorf("read file: %w", err)
						}
						jsonData = string(data)
					}
					if err := json.Unmarshal([]byte(jsonData), &raw); err != nil {
						return fmt.Errorf("parse JSON input: %w", err)
					}
					body = raw
				} else {
					m := map[string]interface{}{}
					if cmd.Flags().Changed("filename") {
						v, _ := cmd.Flags().GetString("filename")
						m["filename"] = v
					}
					if len(m) > 0 {
						body = m
					}
				}

				// Execute request
				resp, err := client.Do("POST", urlPath, query, body)
				if err != nil {
					return err
				}
				if err := CheckError(resp); err != nil {
					return err
				}
				respBody, err := ReadBody(resp)
				if err != nil {
					return fmt.Errorf("read response: %w", err)
				}

				// Handle --quiet
				quiet, _ := cmd.Root().PersistentFlags().GetBool("quiet")
				if quiet {
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err == nil {
						// Handle paginated list responses ({"data": [...]})
						if items, ok := data["data"].([]interface{}); ok {
							for _, item := range items {
								if m, ok := item.(map[string]interface{}); ok {
									for _, key := range []string{"id", "name", "key"} {
										if v, ok := m[key]; ok {
											fmt.Fprintln(os.Stdout, v)
											break
										}
									}
								}
							}
							return nil
						}
						// Handle single resource responses
						for _, key := range []string{"id", "name", "key"} {
							if v, ok := data[key]; ok {
								fmt.Fprintln(os.Stdout, v)
								return nil
							}
						}
					}
					fmt.Fprintln(os.Stdout, string(respBody))
					return nil
				}

				switch OutputFormat(outputFlag) {
				case OutputJSON:
					var pretty interface{}
					json.Unmarshal(respBody, &pretty)
					return PrintJSON(os.Stdout, pretty)
				default:
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err != nil {
						return fmt.Errorf("parse response: %w", err)
					}
					PrintDetail(os.Stdout, data)
				}
				return nil
			},
		}
		c.Flags().String("catalog-name", "", "Name of the catalog.")
		_ = c.MarkFlagRequired("catalog-name")
		c.Flags().String("filename", "", "Optional filename hint for the uploaded file.")
		c.Flags().String("json", "", "JSON input (raw string or @filename or - for stdin)")

		// Apply overrides
		if fn, ok := runOverrides["createUploadUrl"]; ok {
			c.RunE = fn(client)
		}
		if fn, ok := commandOverrides["createUploadUrl"]; ok {
			fn(c)
		}
		cmd.AddCommand(c)
	}

	return cmd
}
