// Code generated by cli-gen. DO NOT EDIT.
package gen

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"
	"strings"

	"github.com/spf13/cobra"
)

func newManifestCmd(client *Client) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "manifest",
		Short: "Client-side query manifest",
	}

	// createManifest
	{
		c := &cobra.Command{
			Use:     "create",
			Short:   "Create table manifest with presigned URLs and security policies",
			Long:    "Returns presigned S3 URLs for the Parquet files backing a table, along with RLS row filters and column masks for the authenticated principal. Used by the duck_access DuckDB extension for secure client-side querying.\n",
			Example: "duck manifest create --schema main --table users",
			RunE: func(cmd *cobra.Command, args []string) error {
				outputFlag, _ := cmd.Flags().GetString("output")
				_ = outputFlag
				urlPath := "/manifest"
				query := url.Values{}
				// Build request body
				var body interface{}
				jsonInput, _ := cmd.Flags().GetString("json")
				if jsonInput != "" {
					var raw interface{}
					jsonData := jsonInput
					if jsonInput == "-" {
						data, err := os.ReadFile("/dev/stdin")
						if err != nil {
							return fmt.Errorf("read stdin: %w", err)
						}
						jsonData = string(data)
					} else if strings.HasPrefix(jsonInput, "@") {
						data, err := os.ReadFile(jsonInput[1:])
						if err != nil {
							return fmt.Errorf("read file: %w", err)
						}
						jsonData = string(data)
					}
					if err := json.Unmarshal([]byte(jsonData), &raw); err != nil {
						return fmt.Errorf("parse JSON input: %w", err)
					}
					body = raw
				} else {
					m := map[string]interface{}{}
					if cmd.Flags().Changed("schema") {
						v, _ := cmd.Flags().GetString("schema")
						m["schema"] = v
					}
					if cmd.Flags().Changed("table") {
						v, _ := cmd.Flags().GetString("table")
						m["table"] = v
					}
					if len(m) > 0 {
						body = m
					}
				}

				// Execute request
				resp, err := client.Do("POST", urlPath, query, body)
				if err != nil {
					return err
				}
				if err := CheckError(resp); err != nil {
					return err
				}
				respBody, err := ReadBody(resp)
				if err != nil {
					return fmt.Errorf("read response: %w", err)
				}

				// Handle --quiet
				quiet, _ := cmd.Root().PersistentFlags().GetBool("quiet")
				if quiet {
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err == nil {
						// Try common ID fields
						for _, key := range []string{"id", "name", "key"} {
							if v, ok := data[key]; ok {
								fmt.Fprintln(os.Stdout, v)
								return nil
							}
						}
					}
					fmt.Fprintln(os.Stdout, string(respBody))
					return nil
				}

				switch OutputFormat(outputFlag) {
				case OutputJSON:
					var pretty interface{}
					json.Unmarshal(respBody, &pretty)
					return PrintJSON(os.Stdout, pretty)
				default:
					var data map[string]interface{}
					if err := json.Unmarshal(respBody, &data); err != nil {
						return fmt.Errorf("parse response: %w", err)
					}
					PrintDetail(os.Stdout, data)
				}
				return nil
			},
		}
		c.Flags().String("json", "", "JSON input (raw string or @filename or - for stdin)")
		c.Flags().String("schema", "main", "")
		c.Flags().String("table", "", "")
		_ = c.MarkFlagRequired("table")

		// Apply overrides
		if fn, ok := runOverrides["createManifest"]; ok {
			c.RunE = fn(client)
		}
		if fn, ok := commandOverrides["createManifest"]; ok {
			fn(c)
		}
		cmd.AddCommand(c)
	}

	return cmd
}
